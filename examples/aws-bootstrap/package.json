{
  "name": "nebula-aws-bootstrap",
  "version": "1.0.0",
  "private": true,
  "license": "MPL-2.0",
  "description": "Run-from-here shim for `nebula bootstrap --provider aws`. The CLI is self-sufficient (builds the bootstrap topology in-process) — no bootstrap.ts/mgmt.ts needed.",
  "dependencies": {
    "@nebula/cli": "file:../../packages/cli"
  }
}
